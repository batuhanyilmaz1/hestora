/// Brand watermark drawn between the raster template and dynamic overlays.
enum ShareCardWatermarkStyle {
  none,
  /// Low-opacity white text for navy / dark gradients.
  light,
  /// Low-opacity dark text for very light templates.
  dark,
}
